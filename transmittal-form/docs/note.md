- Problem in the File That will be imported because we are Using AI right for the part where we let AI generate the Document # BUt the problem is that when the quota hit or AI hallucinate I see some errors if that happens we should have a fallback to the old way of generating the Document # and the Thing is that its hard to find it inside the docuemnt that for example have been an image then converted into pdf or docs. Or its very hard for some reason, Can you implement a fallback that is Good and somewhat Good to look in here?

- Adding Column for getting a custom AI API Key from user logged in if they can.
- Adding
