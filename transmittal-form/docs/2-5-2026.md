- Audit current Browse Drive flow and define UX for selectable import list (modal/list, search, folder link support). - DONE
- Implement Browse Drive list UI and selection flow wired to Drive API (add selected files to items). - DONE
- Add a sending API endpoint protected by a static token (define route, request/response, error model). - IN PROGRESS
- Save transmittals to DB and enable edit/update flows (add update endpoint + load/save in UI).
- History tab: ensure scrollable and responsive layout on small screens.
- Verify transmittal ID generation is unique per day (sequence + collision handling).
