- user will see the extension
- then when they click the extension first the setup we need to label the menu with number so user can be sure where to start.
- the first is they will select which tabs the automation will work. remeber the automation is just (DETAILS OF THE AUTOMATION Maybe refer also to the old documentation we have in here).
- then user will first need to select which row are the column headers, then the starting row for the Data which the user will put the data then again for merged row are like in here ok since there are rows in the company that have different rows inside which are ok it just means that the data company have many maybe seller, or buyer
- Then user can put multiple emails where the email of reminders will be sent remember the google sheet is created already and by the user and we just put the appscript for automating sending emails based on the data in the given sheet.
- The cells have created conditional formatting where the columns like NOTARY DATE OF DOCUMENT user will put here then data will be created here in the "DST Due Date" column (can have different but have a phrase like this) it have a Remaining days column beside next a Remaining Days, Status (in here I want you to get the dropdown list in here), Reminder (created immediately and what we will use for sending email content). Also there is a similar in here next column but can have name like "CGT/ DOD Due Date" that will with same idea send emails based on the 30 days after the notary date also in here the DST will be sent every 5th of the following month. the CGT will be sent 30 days after notary.
